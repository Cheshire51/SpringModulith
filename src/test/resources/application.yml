spring:
  datasource:
    username: myuser
    password: mysecretpassword
    url: jdbc:postgresql://postgres:5432/some-postgres
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

#  docker:
#    compose:
#      skip:
#        in-tests: false

logging:
  level:
    org.springframework.modulith : DEBUG