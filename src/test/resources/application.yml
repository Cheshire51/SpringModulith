spring:
  datasource:
    password: ${PASSWORD}
    username: ${USERNAME}
    url: jdbc:postgresql://localhost:5432/some-postgres

  docker:
    compose:
      skip:
        in-tests: false

logging:
  level:
    org.springframework.modulith : DEBUG